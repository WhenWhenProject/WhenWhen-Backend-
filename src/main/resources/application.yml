spring:
  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true

    open-in-view: false

  profiles:
    include: aws

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace


